# BitCons 三阶段优化进度报告

> 报告日期：2026-08-28  
> 数据集与模型：CIFAR-10 / ResNet18  
> 统一探索设置：seed 4243，110 epoch，`L_inf epsilon=8/255`  
> 覆盖批次：初始完整实验、低位辅助流探索、BPDA 低位平面探索

## 1. 总体结论

三轮探索完成了从“旧 BitCons 组合模块”到“低位辅助流”，再到“将位平面处理纳入被攻击模型”的方法收敛。整体进度不是连续调参得到大幅提升，而是通过两轮负结果逐步排除无效模块、修复威胁模型和评估协议，第三轮才得到小幅正向信号。

截至当前，可以给出以下结论：

1. **原始 BitCons Core/Full 不成立。** 第一阶段在 PGD-AT、TRADES、MART、RPAT 上都没有提高标准鲁棒性。Core 跨方法平均 Clean 提高 `2.58` 点，但 AutoAttack 下降 `2.47` 点；Full 更差。
2. **Contrast 已被明确淘汰。** 第一阶段 PGD-AT 平衡消融中，开启 Contrast 平均使 AutoAttack 下降 `2.27` 点，TRADES Full 甚至从 `49.27%` 降到 `21.44%`。
3. **低位辅助流也没有解决问题。** 第二阶段把位平面从 `[3,4,5]` 改成 `[0,1,2]`、降低权重并统一 teacher 后，最好的 Align-only `alpha=0.10` 仍比 Base 低 `1.66` AA 点；更高权重和 Mask-CE 导致明显训练坍塌。
4. **第三阶段出现了首个一致的正向鲁棒信号。** 把低位变换作为模型的一部分并进行 BPDA 自适应训练/评估后，P0、P01、P012 在 PGD、C&W、AutoAttack 上均略高于同轮 Corrected PGD Base。P01 的 AA 提高 `0.31`，P012 的 PGD-50 提高 `0.47`。
5. **当前仍未达到论文有效性门槛。** 第三阶段提升很小，只有单 seed，并且当前没有 quantize-only 对照，尚不能证明收益来自位平面清零而非普通 8-bit 量化。当前结果应表述为“方向从负转为弱正”，不能表述为“方法已经显著有效”。

当前最合理的方法状态是：**暂停 Mask-CE、Align、Contrast，将 BPDA P01/P012 作为候选基础模块，先完成量化因果对照和多 seed 验证。**

## 2. 三阶段实验完成情况

| 阶段 | 计划组数 | 完成训练 | 完整 best 评估 | 状态 |
|---|---:|---:|---:|---|
| 第一阶段：初始完整实验 | 17 | 17 | 17 | 全部完成 |
| 第二阶段：低位辅助流探索 | 8 | 7 | 6 | 1 组评估中断，1 组训练中止 |
| 第三阶段：BPDA 低位平面 | 4 | 4 | 4 | 全部完成 |
| 合计 | 29 | 28 | 27 | 可用完整评估 27 组 |

第二阶段的准确状态为：

- Base、Align P012 A010/A025/A050、Core P012 A010/A025 共 6 组完成训练和完整评估。
- Align P01 A025 完成 110 epoch 训练，评估完成 Clean、PGD-10/20/50，随后在 C&W 阶段中断，因此没有 `eval_results_best.txt`，不进入完整主表。
- Full P012 A025 在 epoch 90/110 停止，不能作为完成实验使用。

其中 `status/align_p01_a025.tsv` 的人工停止说明写为“epoch 100/110”，但训练日志、`final_model.pt` 和 `training_report.txt` 均证明该组已经完成 110 epoch；总报告按实际产物判定为“训练完成、评估中断”。

## 3. 方法演化主线

| 阶段 | 方法定义 | 主要问题 | 阶段决策 |
|---|---|---|---|
| 1. 原始 BitCons | `[3,4,5]` 掩码；Mask-CE + Align；可选 Contrast | 位变换过强、BN 污染、对齐语义不统一、checkpoint 不公平、评估非自适应 | 停用 Contrast，转向低位和统一语义 |
| 2. 低位辅助流 | `[0,1,2]`；最终 adv teacher；降低 alpha；BN 隔离 | `mask(adv)` 可越出威胁球；BN train/eval 语义错位；辅助梯度冲突；高权重坍塌 | 停止辅助流堆叠，重新定义位平面为模型组成 |
| 3. BPDA 位平面 | `g(x)=f(mask(x))`；真实 mask 前向；STE/BPDA 反向 | 增益很小；量化与位清零未拆分；单 seed；创新性不足 | 保留 P01/P012 候选，先做 quantize-only 和多 seed |

这条演化链的核心变化是：研究问题从“辅助损失能否迫使特征对齐”收缩为“低位输入变换本身在完整自适应攻击下是否提供真实增益”。第三阶段的评估口径最可信，应作为后续实验的起点。

## 4. 第一阶段：初始完整实验

### 4.1 实验目的与设置

第一阶段运行编号为 `cifar10_r18_s4243_110e`，完成 17 次训练和 17 次 best checkpoint 完整评估，包括：

- PGD-AT、TRADES、MART、RPAT 四种宿主方法的 Base/Core/Full 对比。
- PGD-AT 上 Mask-CE、Align、Contrast 的完整 `2 x 2 x 2` 八组合消融。
- 位平面为 `[3,4,5]`，最大 8-bit 改变量 `56/255`。
- Mask-CE 固定 `label_smoothing=0.5`，BitCons alpha 为 `1.0`，warmup 100 epoch。

### 4.2 主对比结果

| Method | Base AA | Core AA | Delta Core | Full AA | Delta Full |
|---|---:|---:|---:|---:|---:|
| PGD-AT | 48.82 | 46.60 | -2.22 | 45.62 | -3.20 |
| TRADES | 49.27 | 45.58 | -3.69 | 21.44 | -27.83 |
| MART | 48.56 | 47.01 | -1.55 | 46.05 | -2.51 |
| RPAT | 48.29 | 45.86 | -2.43 | 45.23 | -3.06 |
| 跨方法平均 | 48.73 | 46.26 | -2.47 | 39.59 | -9.14 |

Core 的跨方法平均 Clean 从 `81.12%` 增至 `83.70%`，但 PGD-50 从 `53.66%` 降至 `50.44%`，AA 从 `48.73%` 降至 `46.26%`。这说明原始 BitCons 主要改变了 clean-robust trade-off，没有带来鲁棒性净增益。

### 4.3 组件消融结论

| 模块 | 平均 Delta Clean | 平均 Delta PGD-50 | 平均 Delta AA | 结论 |
|---|---:|---:|---:|---|
| Mask-CE | +1.14 | -0.80 | -0.96 | 提高 Clean，损害鲁棒性 |
| Align | -0.38 | -0.27 | -0.07 | 平均伤害最小，但未形成净增益 |
| Contrast | -0.08 | -3.02 | -2.27 | 明确有害，应停用 |

单独比较时，Align-only 是最接近 Base 的候选：Clean `83.30%`、AA `47.65%`，但仍比 PGD-AT Base 的 `48.82%` 低 `1.17` 点。Mask-only AA 低 `1.92`，Contrast-only AA 低 `3.34`。

### 4.4 第一阶段定位的问题

1. `[3,4,5]` 清零最多改变 `56/255`，远大于 `8/255` 威胁半径。
2. 主流、masked 流、unreliable-bit 流共享 BN，辅助前向持续污染 running statistics。
3. PGD-AT/RPAT 与 TRADES/MART 的 Align teacher 定义不同。
4. 不同宿主的 Contrast lambda 相差 1000 倍，TRADES 的 `1.0` 明显过强。
5. Base 按标准 PGD-10 选 best，BitCons 按非自适应 masked PGD-10 选 best，语义不公平。
6. masked evaluation 不是对 `f(mask(x))` 的自适应攻击，不能证明测试时防御有效。

第一阶段的价值主要是完成了模块否证：Contrast 被淘汰，Mask-CE 不再被视为鲁棒性模块，Align 成为唯一值得整改的辅助候选。

## 5. 第二阶段：低位辅助流探索

### 5.1 相对第一阶段的优化

第二阶段运行编号为 `lowbit_explore_20260828_015531`，进行了以下修改：

1. planes 从 `[3,4,5]` 改为 `[0,1]` 或 `[0,1,2]`，单次 8-bit 清零最大变化降至 `3/255` 或 `7/255`。
2. PGD-AT 的 BitCons 输入统一为最终 adversarial image，不再随机混合 clean/adv。
3. Align teacher 统一为同一次最终 adversarial forward 的 detached logits。
4. Base 与 BitCons 统一按标准 PGD-10 选择 best。
5. Contrast lambda 统一为 `0.001`，主筛选先关闭 Contrast。
6. alpha 从旧 `1.0` 下调到 `0.10/0.25/0.50`，warmup 调整为 60 epoch。
7. 增加 loss component 日志，记录 host、Mask-CE、Align、Contrast 和加权辅助损失。

### 5.2 完整评估结果

| 实验 | Clean | PGD-10 | PGD-20 | PGD-50 | C&W | AA | Delta AA vs Base |
|---|---:|---:|---:|---:|---:|---:|---:|
| PGD-AT Base | **82.96** | **53.50** | **52.71** | **52.47** | **51.04** | **48.84** | 0.00 |
| Align P012 A010 | 81.48 | 52.04 | 51.46 | 51.42 | 49.18 | 47.18 | -1.66 |
| Align P012 A025 | 69.28 | 44.79 | 44.32 | 44.27 | 42.34 | 40.61 | -8.23 |
| Align P012 A050 | 70.10 | 43.83 | 43.47 | 43.42 | 41.04 | 39.14 | -9.70 |
| Core P012 A010 | 64.25 | 43.76 | 43.42 | 43.27 | 35.77 | 34.45 | -14.39 |
| Core P012 A025 | 58.84 | 38.24 | 37.87 | 37.85 | 30.45 | 28.71 | -20.13 |

Align P01 A025 的完整评估中断，已有部分结果为 Clean `70.44%`、PGD-10 `45.81%`、PGD-20 `45.33%`、PGD-50 `45.29%`。这些数值已经明显低于 Base，但由于 C&W/AA 未完成，不进入上表。

### 5.3 第二阶段得到的新认识

**Align 存在明显权重阈值。** A010 能完成正常训练，但仍低于 Base；A025/A050 在 warmup 过程中发生性能坍塌。辅助 loss 的标量虽然不大，梯度方向仍可能持续破坏主目标，说明不能只根据 loss 数值调 alpha。

**Mask-CE 在当前组合中强度过大。** Core A010/A025 的 AA 分别下降 `14.39/20.13` 点，远比 Align-only 严重。`label_smoothing=0.5` 本身没有修改，但 Mask-CE 与 Align 共用较大总体权重时已经不是轻量正则。

**BN 隔离实现引入了新语义差异。** 当时为了阻止 running-stat 写入，把辅助流 BN 切换到 eval，导致 teacher 使用 train BN batch statistics、student 使用 eval BN running statistics。Align 不仅对齐位平面差异，还被迫对齐 BN 函数差异。

**`mask(adv)` 仍可能越出威胁球。** PGD 已经允许 `8/255` 变化，再清除低三位最多继续向下约 `7.5/255`，组合样本相对 clean 最多可达约 `15.5/255`。因此“低三位小于 `8/255`”不能自动保证辅助样本仍属于声明的威胁集合。

**确定性清零带来单向变暗偏置。** `[0,1,2]` 的平均变化不是对称噪声，而是倾向减小像素值。这可能使辅助任务学习固定亮度偏移，而不是低位扰动不变性。

第二阶段说明，仅替换为低位、降低 alpha 和统一 teacher 仍不足以挽救辅助损失方案。问题已经从“参数不合适”上升为“辅助流训练目标和威胁模型定义不合适”。因此提前停止剩余实验是合理的，没有必要补跑已经明显坍塌的 Full。

## 6. 第三阶段：BPDA 低位平面探索

### 6.1 相对第二阶段的结构性修正

第三阶段运行编号为 `bpda_explore_20260828_123058`，不再继续调辅助权重，而是重新定义防御模型：

```text
g(x) = f(mask_low_bits(x))
```

具体变化：

1. 位平面处理成为训练和推理模型的一部分。
2. 前向使用真实的 8-bit round + low-bit mask，反向使用 identity STE/BPDA。
3. 训练 PGD、测试 PGD/C&W/AutoAttack 都直接攻击完整包装模型。
4. 旧 Mask-CE、Align、Contrast 全部关闭，避免辅助梯度和 BN 双流问题。
5. PGD 生成训练样本时临时使用模型 eval mode，结束后恢复 train mode，避免攻击内部多次前向污染 BN running statistics。
6. best 仍统一按标准 PGD-10 选择，只完整评估 best，final 仅保存。

代码中的 Mask-CE 仍保留固定 `label_smoothing=0.5`，没有被修改；由于本阶段 `bitcons=false`，它不参与第三阶段训练。主损失为普通 CE。

### 6.2 完整结果

| 实验 | Clean | PGD-10 | PGD-20 | PGD-50 | C&W | AA | Delta AA |
|---|---:|---:|---:|---:|---:|---:|---:|
| Corrected PGD Base | 83.97 | 53.00 | 51.86 | 51.61 | 50.79 | 48.39 | 0.00 |
| BPDA P0 | 83.62 | 53.30 | 52.23 | 51.95 | 50.98 | 48.59 | +0.20 |
| BPDA P01 | **83.99** | 53.35 | 52.22 | 51.94 | 50.86 | **48.70** | **+0.31** |
| BPDA P012 | 83.84 | **53.55** | **52.30** | **52.08** | **51.02** | 48.60 | +0.21 |

相对本轮 Corrected Base：

- P0：PGD-50 `+0.34`，C&W `+0.19`，AA `+0.20`，Clean `-0.35`。
- P01：PGD-50 `+0.33`，C&W `+0.07`，AA `+0.31`，Clean `+0.02`。
- P012：PGD-50 `+0.47`，C&W `+0.23`，AA `+0.21`，Clean `-0.13`。

P01 当前具有最好的 Clean-AA 权衡，P012 具有最好的 PGD/C&W 数值。但 P01 与 P012 的 AA 只差 `0.10`，位数增加也没有形成稳定单调关系，不能宣称低两位或低三位已经确定最优。

### 6.3 结果可信度

四组均满足 PGD-10 > PGD-20 > PGD-50 > AutoAttack，C&W 和 AA 也能继续降低准确率，未出现最直接的梯度遮蔽迹象。AutoAttack 包含 Square Attack，因此本轮结果比旧 masked evaluation 更可信。

但 BPDA 的 identity gradient 仍是近似梯度。正式论文需要补多 restart PGD、不同 step size、迁移/黑盒攻击，并检查其他合理 BPDA surrogate。当前只能说没有观察到明显异常，不能宣称已经完全排除 obfuscated gradients。

## 7. 三阶段基线与最佳候选对照

不同阶段代码语义发生变化，下面的跨阶段数值用于观察方向，不能当作严格单变量实验。

| 阶段 | 同阶段 Base Clean | 同阶段 Base AA | 最佳新候选 | 候选 Clean | 候选 AA | 同阶段 Delta AA |
|---|---:|---:|---|---:|---:|---:|
| 1. 原始 BitCons | 82.96 | 48.82 | Align-only | 83.30 | 47.65 | -1.17 |
| 2. 低位辅助流 | 82.96 | 48.84 | Align P012 A010 | 81.48 | 47.18 | -1.66 |
| 3. BPDA 位平面 | 83.97 | 48.39 | BPDA P01 | 83.99 | 48.70 | +0.31 |

第一、二阶段 Base AA 只差 `0.02`，说明旧训练语义下基线复现稳定。第三阶段 Corrected Base 的 AA 下降到 `48.39`，主要比较口径也发生了变化：PGD 生成阶段的 BN 模式被修正。第三阶段的增益必须只相对同轮 Corrected Base 计算，不能用旧 Base 的 `48.82/48.84` 替换。

从同阶段 Delta AA 看，优化方向经历了 `-1.17 -> -1.66 -> +0.31`。这表明结构性重定义比继续调辅助损失更有效，但 `+0.31` 仍没有超过预先设置的 `+0.5` 单 seed 继续门槛。

## 8. 问题闭环与当前状态

| 问题 | 首次发现 | 已采取措施 | 当前状态 |
|---|---|---|---|
| `[3,4,5]` 变化过强 | 阶段 1 | 改为 P0/P01/P012 | 已修正位选择，最优位数未确认 |
| Contrast 严重损害鲁棒性 | 阶段 1 | 阶段 2 主筛选关闭，阶段 3 全关 | 已淘汰当前实现 |
| Mask-CE 提 Clean、降鲁棒 | 阶段 1 | 降权筛选后仍坍塌，阶段 3 关闭 | 当前停用 |
| Align teacher 语义不统一 | 阶段 1 | 阶段 2 统一 final adv teacher | 已统一，但 Align 仍无效 |
| checkpoint 选择不公平 | 阶段 1 | 统一使用标准 PGD-10 best | 已修正 |
| masked evaluation 非自适应 | 阶段 1 | 阶段 3 攻击完整 BPDA 模型 | 已修正 |
| 辅助前向污染 BN | 阶段 1 | 阶段 2 隔离、阶段 3 删除双流 | 主路径已规避 |
| BN 隔离引入 train/eval 差异 | 阶段 2 | 修正工具；阶段 3 不使用辅助流 | 当前 BPDA 路径不受影响 |
| `mask(adv)` 越出威胁球 | 阶段 2 | mask 纳入被攻击模型，由攻击围绕原输入约束 | 当前模型定义下已闭环 |
| 量化与位清零贡献混杂 | 阶段 3 | 尚未加入 quantize-only | **未解决，下一优先级最高** |
| 单 seed 无法证明显著性 | 全阶段 | 暂按用户要求单 seed 探索 | **正式实验前未解决** |
| test set 逐 epoch 选 best | 全阶段 | 尚未划 validation | **论文协议未解决** |

## 9. 模块去留结论

### 9.1 当前保留

- **自适应 BPDA 包装与评估协议**：这是三阶段中最重要的有效修正，应作为后续所有位平面方案的底线。
- **P01**：当前最高 Clean 和 AutoAttack，作为综合候选。
- **P012**：当前最高 PGD-10/20/50 和 C&W，作为强度对照。
- **统一标准 best 语义**：继续保留，不能恢复 masked best。

### 9.2 当前停用

- **Contrast**：已有跨组合和跨宿主一致负证据。
- **Mask-CE**：只观察到 Clean 收益，鲁棒性下降；第二阶段更导致严重坍塌。
- **Align 辅助流**：低权重仍未超过 Base，高权重导致优化冲突。
- **非自适应 masked 指标**：只可作为诊断，不得进入论文主表。

### 9.3 尚待因果验证

- 低位清零是否优于普通量化。
- P01 与 P012 的真实优劣。
- 小幅 AA 增益是否能跨 seed 复现。
- BPDA identity surrogate 是否足够覆盖当前不可微变换。

## 10. 代码与工程优化进度

三阶段探索不仅调整了实验参数，也完成了以下工程修正：

1. epoch 统一为 110，学习率衰减点统一为 100/105。
2. 所有主比较统一使用标准 PGD 指标选择 best；final 只保存、不评估。
3. 数据加载优先检查本地 CIFAR-10，已有数据时不重复下载。
4. BitCons、Contrast 和各消融项采用独立开关，配置语义得到统一。
5. 增加 loss component 日志和每任务 status/manifest，便于识别完成、停止和失败状态。
6. 增加 BPDA 模型包装器与 checkpoint 重建支持。
7. PGD 增加攻击阶段模型模式保护，避免 BN running statistics 被重复攻击前向污染。
8. BPDA 脚本实现双 GPU 顺序队列，一次启动完成训练和 best 完整评估。
9. 第三阶段启动前 34 项单元测试全部通过，shell 语法和差异检查通过。

工程完整性已经足以继续受控探索，但论文协议仍缺 validation split、多 seed、攻击多 restart 和结果统计汇总。

## 11. 下一阶段最小方案

下一轮不应重新扩大到四种宿主方法。最小因果实验应先加入：

| 组 | 变换 | 是否已有结果 | 目的 |
|---|---|---|---|
| Corrected PGD Base | 无 | 已有 | 同轮基线 |
| Quantize-only | 只 round 到 8-bit，不清零 | 缺失 | 分离量化收益 |
| BPDA P01 | round + 清零 bit 0,1 | 已有 | 当前 Clean/AA 候选 |
| BPDA P012 | round + 清零 bit 0,1,2 | 已有 | 当前 PGD/C&W 候选 |

如果代码和其他设置保持完全一致，探索阶段只需补跑 Quantize-only 即可完成第一层因果判断。判断规则：

1. 若 Quantize-only 与 P01/P012 相当，当前收益主要来自量化，位平面创新点不成立。
2. 若 P01/P012 稳定高于 Quantize-only 至少 `0.3--0.5` AA 点，才说明低位选择可能有独立价值。
3. 若所有候选相对 Base 仍不足 `+0.5`，应优先改进机制，而不是扩展宿主方法。

通过 quantize-only 后，再对 Base、Quantize-only、最终一个 BPDA 候选运行至少 3 seeds，并报告 mean +/- std。正式论文阶段还必须：

1. 从训练集固定划分 validation，用 validation PGD 选择 checkpoint。
2. 冻结超参数后只对 test 做一次完整评估。
3. PGD/C&W 使用多 restart，并补充迁移或额外黑盒攻击。
4. 扩展到 CIFAR-100、至少一个更宽模型，再验证 TRADES/MART 等宿主兼容性。

## 12. 论文工作当前成熟度

### 已经足以写入论文的方法学内容

- 原始 BitCons 各模块的系统消融和失败分析。
- 非自适应位平面评估为何会高估鲁棒性的讨论。
- 从辅助损失方案转向完整 BPDA 防御模型的设计逻辑。
- 对位平面强度、BN、威胁球和 checkpoint 语义的实现分析。

### 还不足以支撑论文主结论

- 没有多 seed 均值和标准差。
- 没有 quantize-only 因果对照。
- 当前最大 AA 增益仅 `+0.31`，效应不足。
- test set 仍参与逐 epoch checkpoint 选择。
- 当前 BPDA 低位清零与已有 bit-depth reduction 的差异和创新性不足。
- 尚未在第二数据集、第二模型和其他宿主方法上验证最终候选。

因此，现有代码和结果已经足以支撑一篇论文的实验框架、问题分析和方法迭代叙事，但**还不足以支撑“提出的方法显著提高对抗鲁棒性”的最终论文结论**。下一阶段的关键不是增加表格数量，而是证明 `P01/P012 > Quantize-only > Base` 或找到更强、可解释的位平面机制。

## 13. 数据与报告索引

- 第一阶段完整报告：`CIFAR10单Seed完整实验数据报告.md`
- 第一阶段汇总：`logs/suite_cifar10_r18_s4243_110e/results.tsv`
- 第二阶段报告：`BitCons低位平面阶段探索报告.md`
- 第二阶段日志：`logs/lowbit_screen_lowbit_explore_20260828_015531/`
- 第三阶段报告：`BPDA低位平面探索实验分析报告.md`
- 第三阶段日志：`logs/bitplane_bpda_bpda_explore_20260828_123058/`
- 第三阶段执行脚本：`run_bitplane_bpda_exploration.sh`

本报告的跨阶段结论只使用各阶段内部的同轮 Base 计算增益；未完成实验不进入完整强攻击主表。

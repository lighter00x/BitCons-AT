# BitCons-AT 项目代码说明

这份文档按“入口 -> 配置 -> 数据/模型 -> 训练方法 -> BitCons -> 评估 -> 输出”的顺序，说明这个论文代码是怎么串起来的。仓库的核心思路是：先跑一个常规对抗训练方法，再在训练循环里额外挂一条 BitCons 分支，用低位比特平面的掩码视图去约束模型。

## 1. 项目结构总览

| 路径 | 作用 |
|---|---|
| [src/train.py](src/train.py) | 训练入口。负责读取配置、构建数据集/模型/优化器/调度器、选择训练方法、跑每个 epoch、评估并保存 checkpoint。实际输出目录是 `outputs/{dataset}_{model}_{method}_{perturbation}_{desc}/seed_{seed}_{timestamp}/`。 |
| [src/eval.py](src/eval.py) | 评估入口。负责根据实验目录恢复配置、加载模型 checkpoint，并运行自然样本、PGD、C&W、AutoAttack，以及可选的 BitCons 测试时掩码评估。当前解析逻辑对目录命名比较敏感，尤其要注意训练时附带的 `desc` 后缀。 |
| [src/common/args.py](src/common/args.py) | 命令行参数定义。所有训练和 BitCons 超参数都从这里进入。 |
| [src/common/config.py](src/common/config.py) | 配置合并器。先读数据集 YAML，再读训练方法 YAML，最后用命令行参数覆盖。 |
| [configs/datasets/*.yaml](configs/datasets) | 数据集级配置，例如数据目录、类别数、batch size、worker 数。 |
| [configs/training/*.yaml](configs/training) | 训练方法级配置，例如 `method`、学习率、攻击步数、`beta`、BitCons 开关和超参数。 |
| [src/datasets/__init__.py](src/datasets/__init__.py) | 根据 `config.dataset` 选择对应的数据加载器。 |
| [src/datasets/cifar.py](src/datasets/cifar.py)、[src/datasets/svhn.py](src/datasets/svhn.py)、[src/datasets/tinynet.py](src/datasets/tinynet.py) | 各数据集的数据增强、下载和 DataLoader 构建。 |
| [src/models/__init__.py](src/models/__init__.py) | 根据 `config.model` 选择模型。 |
| [src/models/resnet.py](src/models/resnet.py)、[src/models/preactresnet.py](src/models/preactresnet.py)、[src/models/wideresnet.py](src/models/wideresnet.py) | 具体网络实现。 |
| [src/attacks/__init__.py](src/attacks/__init__.py) | 导出 PGD、TRADES、MART 等对抗样本生成函数。 |
| [src/losses/bitcons.py](src/losses/bitcons.py) | BitCons 的核心工具：比特平面掩码、对齐损失、warmup 权重。 |
| [src/losses/trades.py](src/losses/trades.py)、[src/losses/mart.py](src/losses/mart.py)、[src/losses/consistency.py](src/losses/consistency.py) | 各方法的基础损失。 |
| [src/training/methods/*.py](src/training/methods) | 具体训练循环。每个方法在自己的主损失上再叠加 BitCons 分支。 |
| [src/training/perturbations/*.py](src/training/perturbations) | 权重扰动方法。当前训练管线实际接入的是 AWP 和 RWP。 |
| [src/evals/*.py](src/evals) | 自然精度、PGD、C&W、AutoAttack，以及 BitCons 掩码评估。 |
| [src/utils/*.py](src/utils) | 日志、checkpoint、曲线绘图。 |
| [run_examples.sh](run_examples.sh)、[run_eval.sh](run_eval.sh) | 仓库自带的批量训练和批量评估示例脚本。 |

## 2. 训练和评估是怎么串起来的

### 2.1 训练入口

训练从 [src/train.py](src/train.py) 开始。

1. `get_args()` 读取命令行参数，包含数据集、模型、训练方法、扰动方法和 BitCons 超参数。
2. `Config.load_from_args()` 先加载 [configs/datasets](configs/datasets) 里的数据集配置，再加载 [configs/training](configs/training) 里的方法配置，最后用命令行参数覆盖。
3. `get_loaders(config)` 从 [src/datasets/__init__.py](src/datasets/__init__.py) 分发到具体数据集加载器。
4. `get_model(config)` 从 [src/models/__init__.py](src/models/__init__.py) 选择网络。
5. `get_optimizer(config, model)` 和 `get_scheduler(config, optimizer)` 在 [src/common/utils.py](src/common/utils.py) 里创建优化器和学习率调度器。
6. `get_criterion(config)` 选择基础分类损失。
7. `get_perturbation(config, model, optimizer, device)` 在 [src/training/perturbations/__init__.py](src/training/perturbations/__init__.py) 里构建 AWP/RWP 权重扰动对象。
8. `get_train_fn(config)` 在 [src/training/methods/__init__.py](src/training/methods/__init__.py) 里选择 `pgd_at_train`、`trades_train`、`mart_train`、`cons_at_train` 或 `rpat_train`。
9. 每个 epoch 里先跑训练函数，再跑自然评估和 PGD-10 评估；如果开了 BitCons，还会额外跑 masked 的自然评估和 PGD-10 评估。
10. `Logger` 会把指标写到 `metrics.csv`，把最好模型和最终模型存到 `checkpoints/`。训练目录实际长这样：`outputs/{dataset}_{model}_{method}_{perturbation}_{desc}/seed_{seed}_{timestamp}/`。

### 2.2 评估入口

评估从 [src/eval.py](src/eval.py) 开始。

1. `--exp` 可以传完整实验路径，也可以传子目录名或主目录名。
2. `load_exp_config()` 会根据目录名推回 `dataset`、`model`、`method`、`perturbation`、`seed`，但当前实现主要按 `dataset_model_method_perturbation` 的主目录名来解析；如果训练时额外带了 `desc` 后缀，评估前需要同步检查这段解析逻辑。
3. `find_checkpoint()` 按 `best_model.pt` 或 `final_model.pt` 找权重。
4. `get_loaders()` 和 `get_model()` 重新构建测试集与模型。
5. `evaluate_natural`、`evaluate_pgd_10`、`evaluate_pgd_20`、`evaluate_pgd_50`、`evaluate_cw`、`evaluate_aa` 逐个跑攻击。
6. 如果加 `--bitcons-test`，会先用 `_BitConsMaskedModel` 把输入做比特平面掩码，再进入模型前向。

## 3. 各代码文件的作用

### 3.1 参数和配置层

- [src/common/args.py](src/common/args.py)：定义所有 CLI 参数。这里不仅有 `dataset`、`model`、`config`，还有 `epsilon`、`alpha`、`n_steps`、`beta`、`perturbation`、`bitcons_planes`、`bitcons_align`、`bitcons_alpha`、`bitcons_warmup`、`bitcons_warmup_schedule` 等。
- [src/common/config.py](src/common/config.py)：把 YAML 和命令行参数合并成一个 `Config` 对象。它的优先级是：数据集 YAML < 训练 YAML < 命令行参数。
- [configs/datasets/cifar10.yaml](configs/datasets/cifar10.yaml)、[configs/datasets/cifar100.yaml](configs/datasets/cifar100.yaml)、[configs/datasets/tinynet.yaml](configs/datasets/tinynet.yaml)：提供数据集级默认值。当前仓库里没有单独的 `svhn.yaml`。
- [configs/training/pgd_at.yaml](configs/training/pgd_at.yaml)、[configs/training/trades.yaml](configs/training/trades.yaml)、[configs/training/mart.yaml](configs/training/mart.yaml)、[configs/training/cons_at.yaml](configs/training/cons_at.yaml)、[configs/training/rpat.yaml](configs/training/rpat.yaml)：提供方法级默认值，并在同一份 YAML 里开启或关闭 BitCons。

### 3.2 数据和模型层

- [src/datasets/__init__.py](src/datasets/__init__.py)：统一入口，根据 `config.dataset` 分发到 CIFAR-10、CIFAR-100、SVHN 或 TinyImageNet 的 loader。
- [src/datasets/cifar.py](src/datasets/cifar.py)：CIFAR-10 / CIFAR-100 的加载器，训练集使用随机裁剪和随机翻转。`cons_at` 场景下会返回两份增强视图。
- [src/datasets/svhn.py](src/datasets/svhn.py)：SVHN 的加载器，训练集会加高斯噪声。`cons_at` 时同样支持双视图返回。
- [src/datasets/tinynet.py](src/datasets/tinynet.py)：TinyImageNet 的加载器，负责下载、解压和构建训练/验证集。
- [src/models/__init__.py](src/models/__init__.py)：支持 `resnet18`、`preactresnet18`、`wrn28_10`、`wrn34_10`。

### 3.3 攻击与损失层

- [src/attacks/pgd.py](src/attacks/pgd.py)：实现 PGD 和 `PGD_v2`。训练里常用 `PGD_v2`，因为它会额外返回第一步的 logits，方便后续对齐。
- [src/attacks/trades.py](src/attacks/trades.py)：实现 TRADES 风格的对抗样本生成。
- [src/losses/trades.py](src/losses/trades.py)：TRADES 基础损失，形式是 `CE(clean) + beta * KL(adv || clean)`。
- [src/losses/mart.py](src/losses/mart.py)：MART 的基础损失，返回 `loss_adv` 和 `loss_robust` 两部分。
- [src/losses/consistency.py](src/losses/consistency.py)：一致性损失，用于 `cons_at`，本质是两个对抗分支之间的 JS divergence。
- [src/losses/bitcons.py](src/losses/bitcons.py)：BitCons 的核心实现。

### 3.4 训练方法层

- [src/training/methods/pgd_at.py](src/training/methods/pgd_at.py)：PGD 对抗训练。先生成对抗样本，再计算分类损失；如果打开 BitCons，就对一部分输入做比特平面掩码并加上对齐损失。
- [src/training/methods/trades.py](src/training/methods/trades.py)：TRADES 训练。先生成 `images_adv`，再算 TRADES 主损失；BitCons 分支作用在 clean stream 上。
- [src/training/methods/mart.py](src/training/methods/mart.py)：MART 训练。主损失是 MART loss，BitCons 也是附加分支。
- [src/training/methods/cons_at.py](src/training/methods/cons_at.py)：Consistency Adversarial Training。输入是成对增强视图，先拼接后做对抗攻击，再对两个 adversarial half 做一致性约束。
- [src/training/methods/rpat.py](src/training/methods/rpat.py)：Robust Perception Adversarial Training。除了基础对抗损失，还会在 `RA_start` 之后加入 perception/插值损失。

### 3.5 权重扰动层

- [src/training/perturbations/awp.py](src/training/perturbations/awp.py)：AWP 实现，先训练一个 proxy model，再把 proxy 和主模型的权重差映射成扰动方向。
- [src/training/perturbations/rwp.py](src/training/perturbations/rwp.py)：RWP 实现，使用双 proxy 和迭代式差分更新。
- [src/training/perturbations/utils.py](src/training/perturbations/utils.py)：更细粒度的层权重计算工具，包含 KL 统计和 layer weight tracker。
- [src/training/perturbations/__init__.py](src/training/perturbations/__init__.py)：当前训练主流程只会返回 AWP 或 RWP。

### 3.6 评估与输出层

- [src/evals/natural.py](src/evals/natural.py)：自然样本准确率和 BitCons 掩码后的自然准确率。
- [src/evals/pgd.py](src/evals/pgd.py)：PGD-10 / PGD-20 / PGD-50，以及 masked PGD 评估。
- [src/evals/cw.py](src/evals/cw.py)：C&W 攻击评估。
- [src/evals/aa.py](src/evals/aa.py)：AutoAttack 评估。
- [src/utils/logger.py](src/utils/logger.py)：记录训练指标，输出 `metrics.csv`、`eval_results.txt`、`training_report.txt`、`metrics.png`、`summary.png`。
- [src/utils/checkpoint.py](src/utils/checkpoint.py)：保存和恢复模型、优化器状态。

## 4. BitCons 具体是怎么工作的

BitCons 的核心在 [src/losses/bitcons.py](src/losses/bitcons.py)，它包含三件事。

### 4.1 比特平面掩码

`apply_bitplane_mask(images, fragile_planes)` 会先把输入从 `[0, 1]` 的浮点图像转成 8-bit 整数，再把指定 bit-plane 清零，最后除回 `[0, 1]`。

- `0` 是最低位 LSB。
- `7` 是最高位 MSB。
- 典型配置是 `[0, 1, 2]` 或 `[3, 4, 5]`。

### 4.2 对齐损失

`bitcons_align_loss(logit_bc, logit_ref, align_type, temperature)` 支持四种形式：

- `js`：Jensen-Shannon divergence，默认值，数值更稳。
- `kl`：KL divergence。
- `mse`：直接对 logits 做 MSE。
- `kl_zscore`：先对 logits 做 Z-score，再算 KL。

### 4.3 warmup 权重

`get_bitcons_weight(config, epoch)` 负责把 BitCons 权重 `alpha` 从 0 逐步升到 `bitcons_alpha`。

- `bitcons_warmup = 0` 时直接使用最终权重。
- `linear` 是线性上升。
- `cosine` 是余弦上升。

### 4.4 BitCons 在训练里挂在哪里

所有方法都会先算自己的主损失，再判断 `config.bitcons`。

- `cons_at`：BitCons 分支会额外加 `loss_bc_ce + loss_bc_align`。
- `pgd_at`、`trades`、`mart`、`rpat`：代码里也计算了 `loss_bc_ce`，但当前实际加入总 loss 的只有 `loss_bc_align`，`loss_bc_ce` 这一项被注释掉了。

这点很重要，因为它说明“文档中的 BitCons”不完全等于“当前仓库里实际参与反向传播的项”。

## 5. 各训练方法的差异

| 方法 | 对抗样本生成 | 主损失 | BitCons 的对齐目标 |
|---|---|---|---|
| PGD-AT | `PGD_v2` | `CrossEntropyLoss` | 对齐到 `logits_orig`，并且 BitCons 输入会在 benign / adv 之间随机混合。 |
| TRADES | `generate_trades()` | `trades_loss()` | 对齐到 `logits_clean.detach()`。 |
| MART | `PGD()` | `mart_loss()` | 对齐到 `logits_clean.detach()`。 |
| Cons-AT | 拼接后的双增强视图再做 PGD | `CrossEntropyLoss + lam * consistency_loss()` | 掩码作用在第一个增强视图上，对齐到 `logits_orig`。 |
| R-PAT | `PGD_v2` | `CrossEntropyLoss + RA loss` | 掩码作用在 clean 输入上，对齐到 `logits_orig`。 |

## 6. 推荐的使用顺序

### 6.1 训练一个默认基线

```bash
python src/train.py --dataset cifar10 --model preactresnet18 --config pgd_at --desc base
```

如果想换方法，只要改 `--config`：

```bash
python src/train.py --dataset cifar10 --model preactresnet18 --config trades --desc base
python src/train.py --dataset cifar10 --model preactresnet18 --config mart --desc base
python src/train.py --dataset cifar10 --model preactresnet18 --config cons_at --desc base
python src/train.py --dataset cifar10 --model preactresnet18 --config rpat --desc base
```

### 6.2 开启 BitCons

最简单的方式是在对应训练 YAML 里把 `bitcons: true` 打开，然后调整下面几个键：

```yaml
bitcons: true
bitcons_planes: [3, 4, 5]
bitcons_align: kl
bitcons_alpha: 1.0
bitcons_warmup: 100
bitcons_warmup_schedule: linear
```

也可以在命令行覆盖：

```bash
python src/train.py --dataset cifar10 --model preactresnet18 --config pgd_at \
  --bitcons_planes 3 4 5 \
  --bitcons_align kl \
  --bitcons_alpha 1.0 \
  --bitcons_warmup 100 \
  --bitcons_warmup_schedule linear
```

### 6.3 开启权重扰动

训练入口支持 `--perturbation awp` 或 `--perturbation rwp`。如果你在 YAML 里把 `perturbation` 设成非 `none`，`get_perturbation()` 就会返回对应对象。

```bash
python src/train.py --dataset cifar10 --model preactresnet18 --config pgd_at --perturbation awp
```

### 6.4 评估训练结果

```bash
python src/eval.py --exp outputs/cifar10_preactresnet18_pgd_at_none_test/seed_42_20260421_120000
```

常见选项：

```bash
python src/eval.py --exp <experiment_folder> --ckpt best
python src/eval.py --exp <experiment_folder> --ckpt final
python src/eval.py --exp <experiment_folder> --all-attacks
python src/eval.py --exp <experiment_folder> --bitcons-test --bitcons-planes 3 4 5
```

## 7. 运行后会生成什么

训练输出目录由 [src/train.py](src/train.py) 组织，结构大致是：

```text
outputs/
  {dataset}_{model}_{method}_{perturbation}_{desc}/
    seed_{seed}_{timestamp}/
      metrics.csv
      eval_results.txt
      training_report.txt
      checkpoints/
        best_model.pt
        final_model.pt
```

`run_examples.sh` 和 `run_eval.sh` 只是把这些命令批量化，方便在多张 GPU 上同时启动训练和评估。

## 8. 读代码时需要注意的几个边界

- `pgd_at`、`trades`、`mart`、`rpat` 的 BitCons 分支里都算了 `loss_bc_ce`，但当前只有 `cons_at` 真正把它加进总损失；其他方法只把对齐损失加进去了。
- [src/common/args.py](src/common/args.py) 里预留了 `cwp` 和 `uawp` 的命令行参数，但当前 `get_perturbation()` 真正返回的只有 `awp` 和 `rwp`。
- [src/eval.py](src/eval.py) 里有一行把 `data_dir` 硬编码成了 `/home/chenyu/ADV/data`，如果你的数据目录不同，需要改这里。
- [src/train.py](src/train.py) 会把 `desc` 拼进主实验目录名，但 [src/eval.py](src/eval.py) 的目录解析没有显式剥离这个后缀；如果你改了 `--desc`，评估前要确认 `load_exp_config()` 仍然能正确识别目录。
- 当前仓库的训练配置里没有单独的 `svhn.yaml`，如果要跑 SVHN，除了代码支持外，还要补对应的数据集配置。

## 9. 一句话总结

这套代码的主线很清楚：`Config` 负责把 YAML 和命令行合成一份配置，`train.py` 负责搭建训练流水线，具体对抗训练方法在 `training/methods` 里决定主损失，BitCons 在 `losses/bitcons.py` 里提供掩码和对齐约束，`eval.py` 负责把实验目录里的 checkpoint 拿出来统一测自然精度和鲁棒精度。